#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Printing all single digit numbes from 0-9
 *
 * Return: always (0)
*/
int main(void)
{
	for (int i = 0; i <= 9; i++)
		printf("%d\n", i);

	return (0);
}
